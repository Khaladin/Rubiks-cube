# top-most EditorConfig file
root = true
[*]
insert_final_newline = true
charset = utf-8
indent_style = space # this is essentially the same as --tabWidth 2 in prettier command
indent_size = 2
# Unix-style newlines with a newline ending every file
end_of_line = lf
